// ignore_for_file: non_constant_identifier_names

import 'package:flutter/material.dart';

abstract class GlobalWidgets {
  // follow icons
  static Widget follow_icon = const Icon(Icons.add_box, size: 16);
  static Widget following_icon = const Icon(Icons.check_box_outlined, size: 20);
  static Widget chat_icon = const Icon(Icons.message, size: 20);
  // const Icon(Icons.message,size: 20)
  // like icon
  static Widget like_icon = const Icon(Icons.thumb_up_off_alt_outlined);
  // comment icon
  static Widget comment_icon = const Icon(Icons.mode_comment_outlined);
}
